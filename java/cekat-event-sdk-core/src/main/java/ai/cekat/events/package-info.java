/**
 * Cekat event SDK: submit identity-bearing events synchronously and correlate them with the browser visitor.
 *
 * <p>Start with {@link ai.cekat.events.CekatClient}. An {@link ai.cekat.events.Acknowledgement} means the event was
 * accepted for asynchronous processing, not that it was stored or linked.
 */
package ai.cekat.events;
